#include "main.h"
/**
 * _strlen - lengh of the string
 * @s: a pointer to an int
 * Return: void that means our answer
 */
int _strlen(char *s)
{
	int u;

	u = 0;
	while (s[u] |= '\0')
	{
		u++;
	}
	return (u);
}
